<Window x:Class="NtePakTool.YouTubeInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="YouTube URL" Height="200" Width="480"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1E1E1E"
        ShowInTaskbar="False">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="8,3"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#0098FF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="Padding" Value="4"/>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Please enter the YouTube URL"
                   Foreground="#FF6B6B" FontWeight="Bold" FontSize="13" Margin="0,0,0,10"/>

        <TextBox x:Name="TxtUrl" Grid.Row="1" Height="28" FontSize="12"
                 VerticalContentAlignment="Center"
                 KeyDown="TxtUrl_KeyDown"/>

        <TextBlock Grid.Row="2" Text="例: https://www.youtube.com/watch?v=XXXXXXXXXXX"
                   Foreground="#666" FontSize="10" Margin="0,4,0,12"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="80" Height="26" Margin="0,0,8,0"
                    Background="#444" Click="Cancel_Click"/>
            <Button Content="OK" Width="80" Height="26"
                    Click="Ok_Click"/>
        </StackPanel>
    </Grid>
</Window>
