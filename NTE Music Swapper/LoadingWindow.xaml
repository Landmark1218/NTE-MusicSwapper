<Window x:Class="NtePakTool.LoadingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NTE Music Swapper" Height="200" Width="460"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="#1E1E1E"
        AllowsTransparency="True">

    <Border BorderBrush="#333" BorderThickness="1" CornerRadius="8" Margin="1">
        <Grid Margin="32,24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="16"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="10"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="12"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Text="NTE Music Swapper"
                       Foreground="White"
                       FontSize="16"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"/>

            <TextBlock Grid.Row="2"
                       Text="Checking assets..."
                       Foreground="#888"
                       FontSize="11"
                       HorizontalAlignment="Center"/>

            <ProgressBar Grid.Row="4"
                         x:Name="PBar"
                         Height="14"
                         Minimum="0" Maximum="100" Value="0"
                         Foreground="#00FF41"
                         Background="#2D2D2D"
                         BorderThickness="0"/>

            <Grid Grid.Row="6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="TxtMessage"
                           Grid.Column="0"
                           Text="Initializing..."
                           Foreground="#888"
                           FontFamily="Consolas"
                           FontSize="9"
                           TextTrimming="CharacterEllipsis"
                           VerticalAlignment="Center"/>
                <TextBlock x:Name="TxtPercent"
                           Grid.Column="1"
                           Text="0%"
                           Foreground="#00FF41"
                           FontFamily="Consolas"
                           FontSize="9"
                           VerticalAlignment="Center"/>
            </Grid>
        </Grid>
    </Border>
</Window>
